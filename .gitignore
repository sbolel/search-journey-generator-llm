# Environment files
.env

# Yarn-specific artifacts
.yarn/cache/               # Cached package archives (regenerated)
.yarn/unplugged/           # Unplugged package sources (regenerated)
.yarn/build-state.yml      # Build state file (transient)
.yarn/install-state.gz     # Installation state (transient)
.yarn-error.log            # Yarn error log

# Node modules (installed via nodeLinker: node-modules)
node_modules/

# TypeScript artifacts
*.tsbuildinfo             # Incremental build info files
dist/                     # Common build output folder
build/                    # Alternative build output folder

# OS generated files
.DS_Store
Thumbs.db
